"""Chapter 1 — a GPT-style language model written from scratch in pure PyTorch.

Nothing in this package may import `transformers`, or use `nn.Transformer`,
`nn.MultiheadAttention`, `nn.LayerNorm`, `F.scaled_dot_product_attention`, or
`bitsandbytes`. The chapter's entire claim is that these were written by hand (CON-5).
"""
