"""Chapter 2 — QLoRA fine-tune of Qwen2.5-Coder-1.5B into a Java code reviewer.

Nothing in this package may read `eval/holdout/` (CON-6). Not to dedupe, not to check
data quality, not to see why the model is failing. The holdout is the only thing that
makes the headline comparison an honest measurement.
"""
