"""Shared evaluation infrastructure: the JSON contract, the scoring harness, the config loader.

This package is the only code shared by all three chapters. The chapters have no runtime
dependency on each other; they meet here, and in the files under `eval/results/`.
"""
