{"code": "private static MultiValueMap<String, ResponseCookie> adaptCookies(\n        HttpResponse response, HttpClientContext context) {\n\n    LinkedMultiValueMap<String, ResponseCookie> result = new LinkedMultiValueMap<>();\n\n    CookieSpec cookieSpec = context.getCookieSpec();\n    CookieOrigin cookieOrigin = context.getCookieOrigin();\n\n    Iterator<Header> itr = response.headerIterator(HttpHeaders.SET_COOKIE);\n    while (itr.hasNext()) {\n        Header header = itr.next();\n        try {\n            List<Cookie> cookies = cookieSpec.parse(header, cookieOrigin);\n            for (Cookie cookie : cookies) {\n                try {\n                    cookieSpec.validate(cookie, cookieOrigin);\n                    result.add(cookie.getName(),\n                            ResponseCookie.fromClientResponse(cookie.getName(), cookie.getValue())\n                                    .domain(cookie.getDomain())\n                                    .path(cookie.getPath())\n                                    .maxAge(getMaxAgeSeconds(cookie))\n                                    .secure(cookie.isSecure())\n                                    .httpOnly(cookie.containsAttribute(\"httponly\"))\n                                    .sameSite(cookie.getAttribute(\"samesite\"))\n                                    .build());\n                }\n                catch (final MalformedCookieException ex) {\n                    // ignore invalid cookie\n                }\n            }\n        }\n        catch (final MalformedCookieException ex) {\n            // ignore invalid cookie\n        }\n    }\n\n    return result;\n}", "context": "Spring WebFlux reactive HTTP client adapter for Apache HttpComponents: converts Set-Cookie response headers into ResponseCookie objects.", "source_repo": "spring-projects/spring-framework", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-framework/commit/69b74d764530ad2dccdb5b9631cfd9946a4d2508", "severity": "major", "category": "npe-risk", "line": 13, "issue": "`context.getCookieSpec()` returns null when no cookie spec was resolved for the exchange, so `cookieSpec.parse(...)` throws a NullPointerException on the first Set-Cookie header and the whole response fails.", "suggested_fix": "Return the empty result early when the spec is missing: `if (cookieSpec == null) { return result; }` right after `context.getCookieSpec()`.", "label_source": "maintainer-fix-commit"}
{"code": "static SQLDialect getDialect(DataSource dataSource) {\n    try {\n        Connection connection = (dataSource != null) ? dataSource.getConnection() : null;\n        return JDBCUtils.dialect(connection);\n    }\n    catch (SQLException ex) {\n        logger.warn(\"Unable to determine dialect from datasource\", ex);\n    }\n    return SQLDialect.DEFAULT;\n}", "context": "Spring Boot jOOQ auto-configuration: detects the SQL dialect of the application's DataSource at startup.", "source_repo": "spring-projects/spring-boot", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-boot/commit/dc8b55c0efb10650a0a82747bed322522b625d2f", "severity": "major", "category": "resource-leak-unclosed-connection", "line": 3, "issue": "The JDBC `Connection` borrowed from the DataSource is never closed, so every dialect lookup leaks a pooled connection and can exhaust the pool.", "suggested_fix": "Use try-with-resources: `try (Connection connection = (dataSource != null) ? dataSource.getConnection() : null) { return JDBCUtils.dialect(connection); }`.", "label_source": "maintainer-fix-commit"}
{"code": "private static MimeType parseMimeTypeInternal(String mimeType) {\n    int index = mimeType.indexOf(';');\n    String fullType = (index >= 0 ? mimeType.substring(0, index) : mimeType).trim();\n    if (fullType.isEmpty()) {\n        throw new InvalidMimeTypeException(mimeType, \"'mimeType' must not be empty\");\n    }\n\n    // java.net.HttpURLConnection returns a *; q=.2 Accept header\n    if (MimeType.WILDCARD_TYPE.equals(fullType)) {\n        fullType = \"*/*\";\n    }\n    int subIndex = fullType.indexOf('/');\n    if (subIndex == -1) {\n        throw new InvalidMimeTypeException(mimeType, \"does not contain '/'\");\n    }\n    if (subIndex == fullType.length() - 1) {\n        throw new InvalidMimeTypeException(mimeType, \"does not contain subtype after '/'\");\n    }\n    String type = fullType.substring(0, subIndex);\n    String subtype = fullType.substring(subIndex + 1);\n    if (MimeType.WILDCARD_TYPE.equals(type) && !MimeType.WILDCARD_TYPE.equals(subtype)) {\n        throw new InvalidMimeTypeException(mimeType, \"wildcard type is legal only in '*/*' (all mime types)\");\n    }\n\n    Map<String, String> parameters = null;\n    do {\n        int nextIndex = index + 1;\n        boolean quoted = false;\n        while (nextIndex < mimeType.length()) {\n            char ch = mimeType.charAt(nextIndex);\n            if (ch == ';') {\n                if (!quoted) {\n                    break;\n                }\n            }\n            else if (ch == '\"' && mimeType.charAt(nextIndex - 1) != '\\\\') {\n                quoted = !quoted;\n            }\n            nextIndex++;\n        }\n        String parameter = mimeType.substring(index + 1, nextIndex).trim();\n        if (parameter.length() > 0) {\n            if (parameters == null) {\n                parameters = new LinkedHashMap<>(4);\n            }\n            int eqIndex = parameter.indexOf('=');\n            if (eqIndex >= 0) {\n                String attribute = parameter.substring(0, eqIndex).trim();\n                String value = parameter.substring(eqIndex + 1).trim();\n                parameters.put(attribute, value);\n            }\n        }\n        index = nextIndex;\n    }\n    while (index < mimeType.length());\n\n    try {\n        return new MimeType(type, subtype, parameters);\n    }\n    catch (UnsupportedCharsetException ex) {\n        throw new InvalidMimeTypeException(mimeType, \"unsupported charset '\" + ex.getCharsetName() + \"'\");\n    }\n    catch (IllegalArgumentException ex) {\n        throw new InvalidMimeTypeException(mimeType, ex.getMessage());\n    }\n}", "context": "Spring core utility that parses MIME type strings (e.g. from HTTP Content-Type/Accept headers, which are client-controlled).", "source_repo": "spring-projects/spring-framework", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-framework/commit/4d6e88dc988e43d90c1e2f52e6694a51f9f8152d", "severity": "major", "category": "index-out-of-bounds", "line": 36, "issue": "When the string has no ';' (`index == -1`), `nextIndex` starts at 0, so a MIME string beginning with '\"' evaluates `mimeType.charAt(-1)` and throws StringIndexOutOfBoundsException on client-controlled header input instead of InvalidMimeTypeException.", "suggested_fix": "Guard the look-behind: `else if (ch == '\"' && (nextIndex == 0 || mimeType.charAt(nextIndex - 1) != '\\\\'))`.", "label_source": "maintainer-fix-commit"}
{"code": "public static Locale parseLocaleString(String localeString) {\n    String[] parts = tokenizeToStringArray(localeString, \"_ \", false, false);\n    String language = (parts.length > 0 ? parts[0] : \"\");\n    String country = (parts.length > 1 ? parts[1] : \"\");\n    validateLocalePart(language);\n    validateLocalePart(country);\n    String variant = \"\";\n    if (parts.length >= 2) {\n        // There is definitely a variant, and it is everything after the country\n        // code sans the separator between the country code and the variant.\n        int endIndexOfCountryCode = localeString.lastIndexOf(country) + country.length();\n        // Strip off any leading '_' and whitespace, what's left is the variant.\n        variant = trimLeadingWhitespace(localeString.substring(endIndexOfCountryCode));\n        if (variant.startsWith(\"_\")) {\n            variant = trimLeadingCharacter(variant, '_');\n        }\n    }\n    return (language.length() > 0 ? new Locale(language, country, variant) : null);\n}", "context": "Spring core utility that parses locale strings such as \"en_US\" or \"de_DE_POSIX\" into java.util.Locale.", "source_repo": "spring-projects/spring-framework", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-framework/commit/e7f89f87c14bb65a83518508a791dc68e681ddd4", "severity": "major", "category": "off-by-one-condition", "line": 8, "issue": "Off-by-one: `parts.length >= 2` enters the variant-extraction branch even when the string has only a language and a country, so the variant is derived from whatever text follows the last occurrence of the country code instead of being empty. A variant exists only when there is a third part.", "suggested_fix": "Only extract a variant when one exists: `if (parts.length > 2)`.", "label_source": "maintainer-fix-commit"}
{"code": "    private String substringWithTrimmedWhitespaceAndQuotesIfQuotesPresent(char chars[], int offset, int count) {\n        int start = offset;\n        int len = count;\n\n        while ((start < (start + len)) && (chars[start] <= ' ')) {\n            start++;\n            len--;\n        }\n\n        while ((start < (start + len)) && ((start + len - 1 < chars.length) && (chars[start + len - 1] <= ' '))) {\n            len--;\n        }\n\n        String value;\n\n        if ((chars.length > 2) && (chars[start] == quoteCharacter) && (chars[start + len - 1] == quoteCharacter)) {\n            value = new String(chars, start + 1, len - 2);\n            if (value.contains(escapedQuoteString)) {\n                value = StringUtils.replace(value, escapedQuoteString, quoteString);\n            }\n        }\n        else {\n            value = new String(chars, offset, count);\n        }\n\n        return value;\n    }", "context": "Spring Batch flat-file reader: tokenizer that splits a delimited (CSV-style) line into fields, trimming whitespace and quotes.", "source_repo": "spring-projects/spring-batch", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-batch/commit/96883023b9e761391ada71759118cab640fce91e", "severity": "major", "category": "index-out-of-bounds", "line": 5, "issue": "For a field that is entirely whitespace the loop trims `len` down to 0, after which `chars[start]` and `chars[start + len - 1]` in the quote check read outside the field (and past the array end), throwing ArrayIndexOutOfBoundsException on blank fields.", "suggested_fix": "Stop trimming before the last character so `len` never reaches 0: `while ((start < (start + len - 1)) && (chars[start] <= ' '))`.", "label_source": "maintainer-fix-commit"}
{"code": "public class SyslogToMapTransformer extends AbstractPayloadTransformer<Object, Map<String, ?>> {\n    private final SimpleDateFormat dateFormat = new SimpleDateFormat(\"MMM dd HH:mm:ss\");\n\n    private Map<String, ?> transform(String payload) {\n        Map<String, Object> map = new LinkedHashMap<String, Object>();\n        Matcher matcher = pattern.matcher(payload);\n        if (matcher.matches()) {\n            try {\n                String facilityString = matcher.group(1);\n                int facility = Integer.parseInt(facilityString);\n                int severity = facility & 0x7;\n                facility = facility >> 3;\n                map.put(FACILITY, facility);\n                map.put(SEVERITY, severity);\n                String timestamp = matcher.group(2);\n                Date date;\n                try {\n                    date = this.dateFormat.parse(timestamp);\n                    Calendar calendar = Calendar.getInstance();\n                    int year = calendar.get(Calendar.YEAR);\n                    int month = calendar.get(Calendar.MONTH);\n                    calendar.setTime(date);\n                    /*\n                     * syslog date doesn't include a year so we\n                     * need to insert the current year - adjusted\n                     * if necessary if close to midnight on Dec 31.\n                     */\n                    if (month == 11 && calendar.get(Calendar.MONTH) == 0) {\n                        calendar.set(Calendar.YEAR, year + 1);\n                    }\n                    else if (month == 0 && calendar.get(Calendar.MONTH) == 1) {\n                        calendar.set(Calendar.YEAR, year - 1);\n                    }\n                    else {\n                        calendar.set(Calendar.YEAR, year);\n                    }\n                    map.put(TIMESTAMP, calendar.getTime());\n                }\n                catch (Exception e) {\n                    /*\n                     * If we can't parse the timestamp, return it as an\n                     * unmodified String. (Postel's law).\n                     */\n                    map.put(TIMESTAMP, timestamp);\n                }\n                map.put(HOST, matcher.group(3));\n                if (StringUtils.hasLength(matcher.group(4))) {\n                    map.put(TAG, matcher.group(4));\n                }\n                map.put(MESSAGE, matcher.group(5));\n            }\n            catch (Exception e) {\n                if (logger.isDebugEnabled()) {\n                    logger.debug(\"Could not decode:\" + payload, e);\n                }\n                map.clear();\n                map.put(UNDECODED, payload);\n            }\n        }\n        else {\n            if (logger.isDebugEnabled()) {\n                logger.debug(\"Could not decode:\" + payload);\n            }\n            map.put(UNDECODED, payload);\n        }\n        return map;\n    }\n}", "context": "Spring Integration transformer that parses incoming syslog messages into a Map; a single instance is shared by all threads processing a message channel.", "source_repo": "spring-projects/spring-integration", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-integration/commit/974fbce911bb69d1af05466706127a495932a7ea", "severity": "major", "category": "thread-unsafe-shared-formatter", "line": 2, "issue": "A single `SimpleDateFormat` instance is shared by every thread calling `transform`, but `SimpleDateFormat` is not thread-safe, so concurrent syslog messages can get corrupted timestamps or throw exceptions.", "suggested_fix": "Do not share the formatter: use a thread-safe `DateTimeFormatter`, or obtain a formatter per call (e.g. from a pool or ThreadLocal).", "label_source": "maintainer-fix-commit"}
{"code": "private void buildRolesReachableInOneOrMoreStepsMap() {\n    this.rolesReachableInOneOrMoreStepsMap = new HashMap<>();\n    // iterate over all higher roles from rolesReachableInOneStepMap\n\n    for (GrantedAuthority role : this.rolesReachableInOneStepMap.keySet()) {\n        Set<GrantedAuthority> rolesToVisitSet = new HashSet<>();\n\n        if (this.rolesReachableInOneStepMap.containsKey(role)) {\n            rolesToVisitSet.addAll(this.rolesReachableInOneStepMap.get(role));\n        }\n\n        Set<GrantedAuthority> visitedRolesSet = new HashSet<>();\n\n        while (!rolesToVisitSet.isEmpty()) {\n            // take a role from the rolesToVisit set\n            GrantedAuthority aRole = rolesToVisitSet.iterator().next();\n            rolesToVisitSet.remove(aRole);\n            visitedRolesSet.add(aRole);\n            if (this.rolesReachableInOneStepMap.containsKey(aRole)) {\n                Set<GrantedAuthority> newReachableRoles = this.rolesReachableInOneStepMap\n                        .get(aRole);\n\n                // definition of a cycle: you can reach the role you are starting from\n                if (rolesToVisitSet.contains(role)\n                        || visitedRolesSet.contains(role)) {\n                    throw new CycleInRoleHierarchyException();\n                }\n                else {\n                    // no cycle\n                    rolesToVisitSet.addAll(newReachableRoles);\n                }\n            }\n        }\n        this.rolesReachableInOneOrMoreStepsMap.put(role, visitedRolesSet);\n\n        logger.debug(\"buildRolesReachableInOneOrMoreStepsMap() - From role \" + role\n                + \" one can reach \" + visitedRolesSet + \" in one or more steps.\");\n    }\n\n}", "context": "Spring Security role hierarchy: precomputes, for every role, the set of roles reachable transitively (e.g. ROLE_ADMIN > ROLE_USER). The hierarchy is configured from a string at startup.", "source_repo": "spring-projects/spring-security", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-security/commit/d3eaef66fcb4f7551caac5514a934fe013050e92", "severity": "major", "category": "infinite-loop", "line": 18, "issue": "Already-visited roles are expanded again: the result of `visitedRolesSet.add(aRole)` is ignored and their reachable roles are re-added to `rolesToVisitSet`. A cycle that does not pass through the starting `role` (e.g. A > B, B > C, C > B) is never detected, so application startup loops forever.", "suggested_fix": "Skip roles already visited: `if (!visitedRolesSet.add(aRole) || !map.containsKey(aRole)) continue;`, and throw CycleInRoleHierarchyException only when `aRole.equals(role)`.", "label_source": "maintainer-fix-commit"}
{"code": "public Collection<TopicPartition> getAssignedPartitions() {\n    ListenerConsumer partitionsListenerConsumer = this.listenerConsumer;\n    if (partitionsListenerConsumer != null) {\n        if (partitionsListenerConsumer.definedPartitions != null) {\n            return Collections.unmodifiableCollection(partitionsListenerConsumer.definedPartitions.keySet());\n        }\n        else if (partitionsListenerConsumer.assignedPartitions != null) {\n            return Collections.unmodifiableCollection(partitionsListenerConsumer.assignedPartitions);\n        }\n    }\n    return null;\n}", "context": "Spring Kafka listener container: public accessor called from arbitrary application threads while the consumer thread concurrently updates partition assignments during rebalances.", "source_repo": "spring-projects/spring-kafka", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-kafka/commit/84a49eb3902f5f120e651dc5a48ab61760dabe67", "severity": "major", "category": "race-condition-shared-collection", "line": 5, "issue": "Returns an unmodifiable *view* over collections the consumer thread mutates during rebalances; callers iterating it on another thread can hit ConcurrentModificationException or see inconsistent state.", "suggested_fix": "Return a snapshot taken under the collection's lock: `synchronized (partitions) { return List.copyOf(partitions); }` (both branches).", "label_source": "maintainer-fix-commit"}
{"code": "private static Assignment getAssignment(IncrOp incrOp, TermFactory termFactory) {\n\n    return incrOp.getValue().intValue() > 0\n            ? Assignment.increment(incrOp.toCqlIdentifier(), termFactory.create(Math.abs(incrOp.getValue().intValue())))\n            : Assignment.decrement(incrOp.toCqlIdentifier(), termFactory.create(Math.abs(incrOp.getValue().intValue())));\n}", "context": "Spring Data Cassandra: builds a CQL counter UPDATE assignment from an increment/decrement operation whose value is a java.lang.Number (callers may pass a long).", "source_repo": "spring-projects/spring-data-cassandra", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-data-cassandra/commit/3dcdd78bf917bde309a278fa991b3b6d7283e078", "severity": "major", "category": "integer-overflow", "line": 3, "issue": "`incrOp.getValue()` is a Number that may hold a long, but `intValue()` truncates it, so large increments/decrements are applied with the wrong magnitude or even the wrong sign.", "suggested_fix": "Use `longValue()` for both the sign check and `Math.abs(...)`.", "label_source": "maintainer-fix-commit"}
{"code": "private void saveChangeSessionId() {\n    String sessionId = getId();\n    if (sessionId.equals(this.originalSessionId)) {\n        return;\n    }\n    if (!this.isNew) {\n        String originalSessionIdKey = getSessionKey(this.originalSessionId);\n        String sessionIdKey = getSessionKey(sessionId);\n        try {\n            RedisIndexedSessionRepository.this.sessionRedisOperations.rename(originalSessionIdKey,\n                    sessionIdKey);\n        }\n        catch (NonTransientDataAccessException ex) {\n            handleErrNoSuchKeyError(ex);\n        }\n        String originalExpiredKey = getExpiredKey(this.originalSessionId);\n        String expiredKey = getExpiredKey(sessionId);\n        try {\n            RedisIndexedSessionRepository.this.sessionRedisOperations.rename(originalExpiredKey, expiredKey);\n        }\n        catch (NonTransientDataAccessException ex) {\n            handleErrNoSuchKeyError(ex);\n        }\n        String originalPrincipalRedisKey = getPrincipalKey(this.originalPrincipalName);\n        RedisIndexedSessionRepository.this.sessionRedisOperations.boundSetOps(originalPrincipalRedisKey)\n                .remove(this.originalSessionId);\n        RedisIndexedSessionRepository.this.sessionRedisOperations.boundSetOps(originalPrincipalRedisKey)\n                .add(sessionId);\n    }\n    this.originalSessionId = sessionId;\n}", "context": "Spring Session Redis repository (inner session class): renames the session keys in Redis when the session id changes, e.g. on login for session-fixation protection. Sessions of anonymous users have no principal name.", "source_repo": "spring-projects/spring-session", "license": "Apache-2.0", "commit_url": "https://github.com/spring-projects/spring-session/commit/12ce8de84eb215bda0b1ad07c5b8198221450e85", "severity": "major", "category": "npe-risk", "line": 24, "issue": "Sessions without an authenticated principal have a null `originalPrincipalName`; `getPrincipalKey(null)` then fails (or writes a bogus principal index key) whenever such a session changes id.", "suggested_fix": "Only update the principal index when a principal exists: wrap the principal-key operations in `if (this.originalPrincipalName != null) { ... }`.", "label_source": "maintainer-fix-commit"}
