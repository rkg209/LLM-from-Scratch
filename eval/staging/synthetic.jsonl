{"code": "public List<User> findByName(String name) {\n    String sql = \"SELECT * FROM users WHERE name = '\" + name + \"'\";\n    return jdbcTemplate.query(sql, new UserRowMapper());\n}", "context": "UserRepository, a plain JDBC DAO used by the admin search screen.", "severity": "critical", "category": "sql-injection", "line": 3, "issue": "`name` is concatenated directly into the SQL string, letting a caller inject arbitrary SQL (e.g. `' OR '1'='1`) to read or modify unrelated rows.", "suggested_fix": "Use a parameterized query: `jdbcTemplate.query(\"SELECT * FROM users WHERE name = ?\", new UserRowMapper(), name)`."}
{"code": "public Document parseUpload(InputStream input) throws Exception {\n    DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();\n    DocumentBuilder builder = factory.newDocumentBuilder();\n    return builder.parse(input);\n}", "context": "UploadController's XML-import handler for a user-supplied file.", "severity": "critical", "category": "xxe-injection", "line": 4, "issue": "The factory is not hardened against external entities, so a crafted XML upload can read local files or trigger SSRF via an XXE payload.", "suggested_fix": "Disable external entities before building: `factory.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\", true)` (and disable external general/parameter entities)."}
{"code": "public class MailService {\n    private static final String SMTP_PASSWORD = \"Summer2023!\";\n\n    public void send(String to, String body) {\n        // ... uses SMTP_PASSWORD to authenticate\n    }\n}", "context": "MailService, the outbound-notification client.", "severity": "critical", "category": "hardcoded-credentials", "line": 2, "issue": "The SMTP password is a literal string compiled into the jar and committed to source control, so anyone with repo or bytecode access has a live credential.", "suggested_fix": "Read the password from environment/secret manager (e.g. `System.getenv(\"SMTP_PASSWORD\")`) and rotate the leaked credential."}
{"code": "public Object loadSession(byte[] data) throws Exception {\n    ObjectInputStream ois = new ObjectInputStream(new ByteArrayInputStream(data));\n    return ois.readObject();\n}", "context": "SessionStore, restoring a serialized session blob from a cache entry.", "severity": "critical", "category": "insecure-deserialization", "line": 3, "issue": "`readObject()` on attacker-influenceable bytes allows arbitrary gadget-chain deserialization, which can lead to remote code execution.", "suggested_fix": "Avoid native Java deserialization for untrusted input; use a look-ahead deserialization filter (`ObjectInputFilter`) or switch to a safe format like JSON with an allowlisted type."}
{"code": "public void convert(String filename) throws IOException {\n    String cmd = \"convert \" + filename + \" output.png\";\n    Runtime.getRuntime().exec(cmd);\n}", "context": "ThumbnailService, converting an uploaded file with ImageMagick.", "severity": "critical", "category": "command-injection", "line": 3, "issue": "`filename` is interpolated into a shell command string; a name like `a.jpg; rm -rf /` executes attacker-controlled shell commands.", "suggested_fix": "Use `Runtime.exec(String[])` or `ProcessBuilder` with the filename as a separate argument (no shell interpretation), after validating it against an allowlist."}
{"code": "public void withdraw(Account acct, BigDecimal amount) {\n    if (acct.getBalance().compareTo(amount) >= 0) {\n        // another thread can withdraw here between the check and the update\n        acct.setBalance(acct.getBalance().subtract(amount));\n    }\n}", "context": "AccountService.withdraw, called concurrently from multiple request threads.", "severity": "critical", "category": "race-condition-check-then-act", "line": 4, "issue": "The balance check and the update are not atomic; two concurrent withdrawals can both pass the check before either updates the balance, overdrawing the account.", "suggested_fix": "Guard the check-then-act with a lock (`synchronized`) or push the decrement into a single atomic/conditional SQL update (`UPDATE accounts SET balance = balance - ? WHERE balance >= ?`)."}
{"code": "public SSLContext trustAllContext() throws Exception {\n    TrustManager[] trustAll = new TrustManager[] { new X509TrustManager() {\n        public void checkClientTrusted(X509Certificate[] c, String a) {}\n        public void checkServerTrusted(X509Certificate[] c, String a) {}\n        public X509Certificate[] getAcceptedIssuers() { return null; }\n    }};\n    SSLContext ctx = SSLContext.getInstance(\"TLS\");\n    ctx.init(null, trustAll, new SecureRandom());\n    return ctx;\n}", "context": "HttpClientConfig, building the SSLContext for an outbound REST client.", "severity": "critical", "category": "improper-certificate-validation", "line": 3, "issue": "The custom TrustManager accepts every certificate unconditionally, disabling TLS server verification and exposing every outbound call to man-in-the-middle attacks.", "suggested_fix": "Remove the trust-all TrustManager and use the platform default (`TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm())`), or pin the expected certificate."}
{"code": "public byte[] loadAvatar(String userId) throws IOException {\n    Path path = Paths.get(\"/data/avatars/\" + userId + \".png\");\n    return Files.readAllBytes(path);\n}", "context": "AvatarController, serving a user's profile picture by id.", "severity": "critical", "category": "path-traversal", "line": 3, "issue": "`userId` is taken from the request and concatenated into the path without sanitization; a value like `../../etc/passwd` escapes `/data/avatars/` entirely.", "suggested_fix": "Resolve and normalize the path, then verify it is still under `/data/avatars/` before reading (or validate `userId` against a strict allowlist pattern first)."}
{"code": "public String formatAddress(Customer customer) {\n    return customer.getAddress().getCity().toUpperCase();\n}", "context": "InvoiceFormatter, building a shipping label string.", "severity": "major", "category": "npe-risk", "line": 2, "issue": "Neither `getAddress()` nor `getCity()` is null-checked; a customer without a saved address throws NullPointerException at invoice time.", "suggested_fix": "Null-check each hop, or use `Optional.ofNullable(customer.getAddress()).map(Address::getCity).map(String::toUpperCase).orElse(\"\")`."}
{"code": "public String readFirstLine(String path) throws IOException {\n    BufferedReader reader = new BufferedReader(new FileReader(path));\n    return reader.readLine();\n}", "context": "ConfigLoader, reading the first line of a small config file.", "severity": "major", "category": "resource-leak-unclosed-stream", "line": 2, "issue": "`reader` is never closed, even on the success path; under load this exhausts file descriptors and the process eventually fails to open new files.", "suggested_fix": "Use try-with-resources: `try (BufferedReader reader = new BufferedReader(new FileReader(path))) { return reader.readLine(); }`."}
{"code": "public class OrderId {\n    private final String value;\n    public OrderId(String value) { this.value = value; }\n\n    @Override\n    public boolean equals(Object o) {\n        if (!(o instanceof OrderId)) return false;\n        return value.equals(((OrderId) o).value);\n    }\n}", "context": "OrderId, a value-object key used in a HashMap<OrderId, Order>.", "severity": "major", "category": "equals-hashcode-contract-violation", "line": 2, "issue": "`equals` is overridden but `hashCode` is not, violating the contract that equal objects must have equal hash codes \u2014 lookups in hash-based collections silently fail to find entries that are logically equal.", "suggested_fix": "Add a matching `hashCode()`: `@Override public int hashCode() { return Objects.hash(value); }`."}
{"code": "public int lastIndexOfEven(int[] values) {\n    for (int i = 0; i <= values.length; i++) {\n        if (values[i] % 2 == 0) return i;\n    }\n    return -1;\n}", "context": "ArrayUtils.lastIndexOfEven, a small helper used by the reporting module.", "severity": "major", "category": "off-by-one-loop-bound", "line": 2, "issue": "The loop condition `i <= values.length` allows `i == values.length`, which indexes one past the array's end and throws ArrayIndexOutOfBoundsException.", "suggested_fix": "Change the bound to `i < values.length`."}
{"code": "public List<String> orderSummaries(List<Order> orders) {\n    List<String> out = new ArrayList<>();\n    for (Order order : orders) {\n        Customer c = customerRepository.findById(order.getCustomerId());\n        out.add(order.getId() + \": \" + c.getName());\n    }\n    return out;\n}", "context": "OrderReportService, building a summary line per order for a nightly report.", "severity": "major", "category": "n-plus-one-query", "line": 3, "issue": "One customer lookup query runs per order inside the loop; for N orders this issues N+1 database round-trips instead of one batched query, and the report job's runtime scales linearly with order volume.", "suggested_fix": "Batch-load the customers up front with a single `findAllById(orderCustomerIds)` call and look them up from an in-memory map inside the loop."}
{"code": "public void transferFunds(Account from, Account to, BigDecimal amount) {\n    from.setBalance(from.getBalance().subtract(amount));\n    accountRepository.save(from);\n    to.setBalance(to.getBalance().add(amount));\n    accountRepository.save(to);\n}", "context": "AccountService.transferFunds, a Spring service method.", "severity": "major", "category": "missing-transactional-annotation", "line": 1, "issue": "The method is not annotated `@Transactional`; if the second `save` throws (e.g. a constraint violation), the debit from `from` is already committed while the credit to `to` never happens, leaving the two accounts inconsistent.", "suggested_fix": "Add `@Transactional` to the method so both saves commit or roll back together."}
{"code": "public class ConfigCache {\n    private static ConfigCache instance;\n    public static ConfigCache getInstance() {\n        if (instance == null) {\n            instance = new ConfigCache();\n        }\n        return instance;\n    }\n}", "context": "ConfigCache, a lazily-initialized singleton read by many request threads.", "severity": "major", "category": "thread-unsafe-singleton", "line": 3, "issue": "The lazy check-then-create in `getInstance()` is not synchronized, so two threads racing on first access can each construct and return a different instance, breaking the singleton guarantee.", "suggested_fix": "Use an eagerly-initialized static field, a static holder class, or synchronize the lazy path (`synchronized` method or double-checked locking with a `volatile` field)."}
{"code": "public int totalCents(int dollars, int cents) {\n    return dollars * 100 + cents;\n}", "context": "MoneyUtils.totalCents, used to convert a dollar amount to a cent count.", "severity": "major", "category": "integer-overflow", "line": 2, "issue": "`dollars * 100` overflows `int` for dollar amounts above roughly 21 million, silently wrapping to a negative or wrong value instead of failing loudly.", "suggested_fix": "Use `long` (or `BigDecimal`/`BigInteger` for currency) for the computation: `return (long) dollars * 100 + cents;` with a `long` return type."}
{"code": "public void processQueue(Queue<Task> tasks) {\n    for (Task task : tasks) {\n        try {\n            task.run();\n        } catch (Exception e) {\n        }\n    }\n}", "context": "TaskWorker.processQueue, a background worker loop.", "severity": "major", "category": "swallowed-exception", "line": 3, "issue": "Every exception from `task.run()` is caught and silently discarded, so a failing task leaves no log entry, no metric, and no trace of why downstream work never happened.", "suggested_fix": "At minimum log the exception with context (`log.error(\"task {} failed\", task.getId(), e)`); consider a dead-letter queue or retry policy for the failed task."}
{"code": "public class Cart {\n    private final List<Item> items = new ArrayList<>();\n    public List<Item> getItems() { return items; }\n}", "context": "Cart, a shopping-cart domain object.", "severity": "major", "category": "mutable-collection-exposed", "line": 2, "issue": "`getItems()` returns the live internal list, so any caller can add, remove, or clear items without going through `Cart`'s own methods, breaking any invariant the class is supposed to enforce.", "suggested_fix": "Return an unmodifiable view: `return Collections.unmodifiableList(items);` (or a defensive copy)."}
{"code": "public void redirectAfterLogin(HttpServletResponse resp, String next) throws IOException {\n    resp.sendRedirect(next);\n}", "context": "LoginController, redirecting to a `next` URL supplied as a query parameter.", "severity": "major", "category": "unvalidated-redirect", "line": 2, "issue": "`next` comes straight from user input and is redirected to without validation, letting an attacker craft a login link that redirects victims to a phishing site after a successful login (open redirect).", "suggested_fix": "Validate `next` is a relative path or matches an allowlist of known hosts before redirecting; otherwise fall back to a safe default page."}
{"code": "public boolean isExpired(String token) {\n    return token.isEmpty() || token == null;\n}", "context": "TokenValidator.isExpired, a guard used before decoding a session token.", "severity": "major", "category": "incorrect-null-check-order", "line": 2, "issue": "`token.isEmpty()` is evaluated before the null check, so a null `token` throws NullPointerException instead of being treated as expired.", "suggested_fix": "Check null first: `return token == null || token.isEmpty();`."}
{"code": "public void login(String username, String password) {\n    log.info(\"login attempt user={} pass={}\", username, password);\n    authenticate(username, password);\n}", "context": "AuthService.login, logging each authentication attempt.", "severity": "minor", "category": "logging-sensitive-data", "line": 2, "issue": "The plaintext password is written to the application log on every login attempt, so anyone with log access (including log-aggregation vendors) can read user passwords.", "suggested_fix": "Drop `password` from the log line entirely, or log only that an attempt occurred: `log.info(\"login attempt user={}\", username);`."}
{"code": "public String joinNames(List<String> names) {\n    String result = \"\";\n    for (String name : names) {\n        result += name + \", \";\n    }\n    return result;\n}", "context": "ReportBuilder.joinNames, formatting a comma-separated name list.", "severity": "minor", "category": "inefficient-string-concatenation-in-loop", "line": 2, "issue": "`result += ...` reallocates and copies a new String each iteration, making the loop O(n^2) in the number of names for what should be a linear operation.", "suggested_fix": "Build the result with a `StringBuilder` and append in the loop, then call `toString()` once at the end."}
{"code": "public class Point {\n    public int x, y;\n    public Point(int x, int y) { this.x = x; this.y = y; }\n}", "context": "Point, used as a key in a `Map<Point, String>` labeling grid cells.", "severity": "minor", "category": "missing-equals-override-in-hashmap-key", "line": 1, "issue": "`Point` has no `equals`/`hashCode` override, so the map compares keys by identity; two `Point` instances with the same `x`/`y` are treated as different keys and lookups by a freshly-constructed `Point` never find an existing entry.", "suggested_fix": "Override `equals` and `hashCode` based on `x` and `y` (or convert to a `record Point(int x, int y) {}`, which generates both for free)."}
{"code": "public boolean anyMatches(List<String> values, String pattern) {\n    for (String value : values) {\n        Pattern p = Pattern.compile(pattern);\n        if (p.matcher(value).matches()) return true;\n    }\n    return false;\n}", "context": "RegexUtils.anyMatches, checking a list of strings against one pattern.", "severity": "minor", "category": "unnecessary-object-creation-in-loop", "line": 3, "issue": "`Pattern.compile(pattern)` recompiles the same regex on every iteration instead of once, which is wasted work proportional to the list size for a pattern that never changes.", "suggested_fix": "Compile the pattern once before the loop: `Pattern p = Pattern.compile(pattern);` moved above the `for`."}
{"code": "public boolean isSessionExpired(long lastSeenMillis) {\n    return System.currentTimeMillis() - lastSeenMillis > 1800000;\n}", "context": "SessionManager.isSessionExpired, checking idle timeout.", "severity": "minor", "category": "magic-number", "line": 2, "issue": "`1800000` (30 minutes in milliseconds) is an unexplained magic number; a reader can't tell the timeout is 30 minutes without doing the arithmetic, and changing the policy means hunting for this literal elsewhere in the codebase.", "suggested_fix": "Extract a named constant, e.g. `private static final long SESSION_TIMEOUT_MS = Duration.ofMinutes(30).toMillis();`."}
{"code": "public String formatInvoiceDate(Date date) {\n    SimpleDateFormat fmt = new SimpleDateFormat(\"MMM dd, yyyy\");\n    return fmt.format(date);\n}", "context": "InvoiceFormatter.formatInvoiceDate, printing a date onto a customer invoice.", "severity": "minor", "category": "incorrect-date-format-locale", "line": 2, "issue": "`SimpleDateFormat` is constructed without a `Locale`, so month names render in the JVM's default locale rather than a locale the business controls \u2014 invoices generated on a non-English server render month names in another language.", "suggested_fix": "Pass an explicit locale: `new SimpleDateFormat(\"MMM dd, yyyy\", Locale.US)`."}
{"code": "public class UppercaseUser extends User {\n    public String getName() {\n        return super.getName().toUpperCase();\n    }\n}", "context": "UppercaseUser, a decorator subclass used in a display layer.", "severity": "minor", "category": "missing-override-annotation", "line": 2, "issue": "`getName()` is missing `@Override`; if `User.getName()`'s signature is later changed or renamed, this method silently becomes a new, unrelated method instead of failing to compile.", "suggested_fix": "Add `@Override` above the method so the compiler enforces the signature match."}
{"code": "public String safe(String value) {\n    if (value != null && value != null) {\n        return value.trim();\n    }\n    return \"\";\n}", "context": "StringUtils.safe, a small null-guarding helper.", "severity": "info", "category": "redundant-null-check", "line": 2, "issue": "The condition `value != null && value != null` repeats the same check twice; the duplicate adds no safety and just confuses a reader into wondering if two different things were meant to be checked.", "suggested_fix": "Remove the duplicate clause: `if (value != null) { ... }`."}
{"code": "public class OrderService {\n    public Order Get_order(String id) {\n        return orderRepository.findById(id);\n    }\n}", "context": "OrderService, a Spring service class.", "severity": "info", "category": "inconsistent-naming-convention", "line": 2, "issue": "`Get_order` mixes PascalCase and snake_case and doesn't follow Java's camelCase method-naming convention, which every other method in the codebase uses \u2014 purely a readability/consistency issue, not a functional bug.", "suggested_fix": "Rename to `getOrder` to match the codebase's camelCase convention."}
{"code": "public interface PaymentGateway {\n    PaymentResult charge(String cardToken, BigDecimal amount);\n}", "context": "PaymentGateway, a public interface implemented by multiple payment providers.", "severity": "info", "category": "missing-javadoc-on-public-api", "line": 1, "issue": "A public interface with no Javadoc gives implementers and callers no documentation of `charge`'s currency assumptions, idempotency behavior, or thrown exceptions, which are exactly the details that differ across implementations.", "suggested_fix": "Add a Javadoc comment on the interface and `charge` describing the currency unit, idempotency guarantees, and failure modes callers must handle."}
